# ============================================================
# Test-only dependencies (not needed in production images)
# Install with: pip install -r requirements-test.txt
# ============================================================
-r requirements.txt

pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
aiosqlite>=0.20.0
